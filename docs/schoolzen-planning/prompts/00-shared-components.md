Read docs/schoolzen-planning/v1/_core/refactor-plan-and-design-system.md and docs/schoolzen-planning/v1/_core/claude-code-implementation-strategy.md in full.

First, build the App Shell (header + sidebar) exactly as specified in docs/schoolzen-planning/v1/_core/shell/app-shell.md, matching docs/schoolzen-planning/v1/_core/shell/app-shell.html pixel-for-pixel: the 13 collapsible module groups (accordion, one open at a time), the role-aware behavior (admin-only groups/items hidden entirely for Teacher; permission-gated items shown with a lock icon and no click handler when that specific permission is false, reading from the real logged-in staff member's roles/permissions per R3 — NOT a UI toggle, that only exists in the static reference for demonstration), the notification bell, and the genuinely responsive off-canvas mobile drawer at the 860px breakpoint. Every other page in the app renders inside this shell — build it before any module's individual pages.

Build the shared Angular component library described in the R5 "Reusable Angular Components" list: app-page-shell, app-data-toolbar, app-status-chip, app-confirm-modal, app-data-table, app-summary-strip.

Match the CSS classes and behavior exactly from docs/schoolzen-planning/v1/payroll/generate-payroll.html — that is the canonical shell/toolbar/table pattern every module reuses (sw-header, sw-body, sb-wrap sidebar, ls-strip summary bar, sw-card-main, sw-toolbar, sw-table-scroll/sw-thead/sw-row flex-table pattern, sw-chip status chips, sw-icon action buttons).

Also read docs/schoolzen-planning/v1/_core/error-handling/README.md and set up the backend error-handling foundation (AppError base class, the 8 category subclasses, the errorHandler middleware, requestId middleware, Winston logger) — place these inside the EXISTING backend/modules/middleware/ and a new backend/modules/errors/ folder, exactly as specified in docs/schoolzen-planning/v1/_core/error-handling/backend/ — and the frontend foundation (error.interceptor.ts, global-error-handler.ts, api-error.model.ts) exactly as specified in docs/schoolzen-planning/v1/_core/error-handling/frontend/. This is shared infrastructure every module will use.

Also read docs/schoolzen-planning/v1/_core/frontend-backend-folder-structure.md in full. The backend project structure (backend/modules/controllers, models, routes, helpers, middleware, services, queues, workers, validators, sockets, as sibling technical-layer folders) ALREADY EXISTS in this project — do not create a new structure or rename/move any existing top-level folder. Every module's files going forward go into a module-named subfolder INSIDE each existing layer folder (e.g. backend/modules/controllers/payroll/, backend/modules/routes/payroll/) exactly as that document describes. Set up the frontend's shared/ component-library skeleton fresh (per the Frontend section of that doc) since that doesn't exist yet.

Do not touch any existing legacy route, component, or schema yet. This is a new, isolated foundation only.
